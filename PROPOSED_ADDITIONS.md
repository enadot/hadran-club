# הצעות להרחבת ה-Design System

מסמך זה מרכז מקומות שבהם העבודה על שדרוג ה-UX נתקלה בפער במערכת העיצוב.
**לא בוצע שינוי באף אחד מהם** — כל פריט כאן ממתין להחלטה.

בכל סעיף: מה חסר, איפה זה נתקל, מה נעשה בפועל כדי לעקוף בלי להמציא ערך,
ומה ההצעה.

---

## 1. מדרגות טיפוגרפיה חסרות — 13px ו-15px

**הפער.** `styles/tokens/typography.css` מגדיר `--text-caption:12px`,
`--text-body-sm:14px`, `--text-body-md:16px`. אין 13px ואין 15px.

**איפה זה נתקל.** הקוד הקיים משתמש בשתי המדרגות האלה **31 פעמים ב-11 קבצים**,
עוד מה-handoff המקורי:

| גודל | מופעים | דוגמאות |
| --- | --- | --- |
| `text-[15px]` | 21 | `ActivateFlow.tsx` (10), `MemberArea.tsx` (4), `merchants/page.tsx` (3) |
| `text-[13px]` | 10 | `Band.tsx` (רכיב `Eyebrow`), `SearchDialog.tsx`, `member`, `balance` |

זה לא חריג נקודתי — זו מדרגה שה-prototypes השתמשו בה בעקביות ושלא הגיעה
לקובץ הטוקנים. הרכיב `Eyebrow` עצמו, שהוא חלק מהמערכת, בנוי על 13px קשיח.

**מה נעשה בפועל.** בקוד החדש לא נוספה אף מדרגה חדשה — נעשה שימוש
ב-`--text-caption` וב-`--text-body-sm` הקיימים. 31 המופעים הישנים **לא שונו**,
כי החלפתם ל-14px/16px היא שינוי ויזואלי ב-11 קבצים ולא החלטה של סוכן.

**ההצעה.** אחת משתיים:

- **א׳ — להכיר במדרגות.** להוסיף `--text-body-xs:13px` ו-`--text-body-base:15px`
  ל-`typography.css`, ולהעביר את 31 המופעים לטוקנים. משמר את המראה במדויק.
- **ב׳ — לוותר עליהן.** למפות 13px→14px ו-15px→16px ולנקות את כל המופעים.
  סולם נקי יותר, אבל משנה מעט את הצפיפות של הטקסט בכל האתר.

ההמלצה שלי: **א׳**. הסולם העברי צפוף ממילא, והקפיצה 12→14→16 גסה מדי לטקסט משני.

---

## 2. אין וריאנט גודל ל-`Badge`

**הפער.** `components/brand/Badge.tsx` מגדיר גודל אחד: `--text-body-sm` (14px)
עם ריפוד `px-3 py-1`.

**איפה זה נתקל.** בשורות של `/benefits` התג יושב על אותה שורה עם שם בית העסק.
ב-390px התווית המלאה ("בלעדי לחברי המועדון") ב-14px גלשה לשורה שנייה והשאירה
כל שורה בגובה אחר — בדיוק מה שרשימה סורקת לא יכולה להרשות לעצמה.

**מה נעשה בפועל.** דריסת `font-size` בלבד דרך `className`, ורק לטוקנים קיימים
(`--text-caption` בנייד, `--text-body-sm` מ-560px). הריפוד נשאר כפי שהוא.
בנוסף נוספה תווית קצרה לכל דרגת הטבה (`short`) ברמת התוכן, לא ברמת הרכיב.

**ההצעה.** להוסיף ל-`Badge` פרופ `size?: "sm" | "md"` שבו `sm` הוא
`--text-caption` עם `px-2 py-0.5`. זה מסיר את הדריסה ומרכז את ההחלטה ברכיב.

---

## 3. אין מצב `loading` ל-`Button`, ואין טוקן תנועה ללולאה

**הפער.** `Button` תומך ב-`disabled` אבל לא במצב "בעבודה". במקביל,
`styles/tokens/motion.css` מגדיר רק משכים ו-easing לתנועה חד-פעמית
(`--duration-fast/base/slow`). אין טוקן לאנימציה מחזורית — ספינר, שלד טעינה,
פעימה.

**איפה זה נתקל.** שלושה טפסים קיבלו מצב שליחה: `/activate`, `/balance`
ו-`/merchants`. לכולם צריך כפתור שמשבית את עצמו ומראה שהוא עובד.

**מה נעשה בפועל.** הכפתור מקבל `disabled`, `aria-busy` וטקסט משתנה
("מפעילים את הכרטיס…"), והספינר נשען על `animate-spin` של Tailwind דרך
`[&>svg]:animate-spin`. ה-Skeleton של shadcn נשען על `animate-pulse` באותו אופן.
שתיהן אנימציות ברירת מחדל של Tailwind, **מחוץ למערכת הטוקנים**.

**ההצעה.** שני חלקים:

1. להוסיף ל-`motion.css` שני טוקנים ללולאת פידבק, למשל
   `--duration-spin:900ms` ו-`--duration-pulse:1.6s`, ולקשור אליהם את
   `animate-spin` / `animate-pulse` ב-`@theme inline`.
2. להוסיף ל-`Button` פרופ `loading?: boolean` שמטפל בשלושת הדברים יחד
   (disabled, aria-busy, ספינר), כדי שלוגיקת המצב לא תשוכפל בכל טופס.

---

## 4. אין טוקנים למשטחים שקופים על רקע ink

**הפער.** אין טוקן ל"לבן-חמים בשקיפות נמוכה מעל `--color-canvas-ink`".

**איפה זה נתקל.** התבנית כבר קיימת בקוד בשלושה ערכי אלפא שונים, כולם קשיחים:

| מקום | ערך |
| --- | --- |
| `Footer.tsx` — קו מפריד | `rgba(241,236,227,.12)` |
| `merchants/page.tsx` — כרטיסי סטטיסטיקה | `rgba(241,236,227,.06)` |
| `merchants/page.tsx` — מסגרת כפתור ghost | `rgba(241,236,227,.28)` |

`241,236,227` הוא בדיוק `--sand-200`, כלומר `--color-on-ink`.

**מה נעשה בפועל.** `CardNumberGuide` — הרכיב היחיד החדש שנזקק לזה — כתוב
`bg-[var(--color-on-ink)]/15`, כלומר דרך הטוקן ולא דרך hex. שלושת המופעים
הישנים **לא שונו**.

**ההצעה.** להגדיר סולם קצר ב-`colors.css`, למשל
`--color-on-ink-subtle` / `--color-on-ink-muted` / `--color-on-ink-border`,
ולהעביר את שלושת המופעים אליו. לחלופין, לאמץ את התחביר
`var(--color-on-ink)/<alpha>` כתקן ולתעד אותו.

---

## 5. אין אייקון `key` בסט

**הפער.** `components/brand/Icon.tsx` מחזיק סט סגור של אייקוני Lucide.
אין בו `Key`.

**איפה זה נתקל.** הבריף מתאר את הכרטיס כ״מפתח״ לעולם של הטבות, וזו בדיוק
המטאפורה של דרגת ההטבה הבלעדית.

**מה נעשה בפועל.** נעשה שימוש ב-`ticket` שכבר קיים בסט. הוא עובד — "כרטיס
כניסה" קרוב מספיק ל"גישה" — אבל `key` היה מדויק יותר.

**ההצעה.** להוסיף `key: Key` למפת האייקונים ולהחליף ב-`BENEFIT_TIERS.exclusive`.
זו תוספת לרשימה קיימת, לא ערך עיצובי חדש — ובכל זאת מתועדת כאן כי הבקשה
היתה לא להוסיף כלום בלי אישור.

---

## 6. אין רכיב Toast / הודעת מערכת

**הפער.** אין ב-`components/` דרך להודיע על תוצאה שאינה קשורה למקום מסוים
בעמוד.

**איפה זה נתקל.** שמירת "פרטים אישיים" ב-`/member` מציגה היום אישור אינליין.
זה עובד. מצבי שגיאה ברשת שאינם קשורים לטופס מסוים — אין להם בית.

**מה נעשה בפועל.** כל הודעות השגיאה החדשות הן אינליין, בתוך הטופס שגרם להן,
עם `role="alert"` — וזה גם הפתרון הנגיש יותר עבור שגיאת טופס. לא נוצר צורך
אמיתי ב-Toast בסבב הזה.

**ההצעה.** לא לעשות כלום כרגע. לרשום כפער ידוע לקראת חיבור ה-API של קהילות
קארד, שבו ייתכנו כשלים אסינכרוניים שאינם שייכים לטופס פתוח.

---

## 7. React Bits — חסום ברמת הרשת

לא פער עיצובי, אבל שייך לכאן: הרישום `@react-bits` נוסף ל-`components.json`
כמבוקש, אבל `reactbits.dev` חסום במדיניות ה-egress של הסביבה הזו
(403 על CONNECT). לכן לא נמשך ממנו אף רכיב, ואף רכיב React Bits אינו בקוד.

`@shadcn` ו-`@magicui` עובדים, ומהם נמשכו הרכיבים שבשימוש.
כשהחסימה תוסר אפשר להריץ:

```bash
npx shadcn@latest add @react-bits/<component>
```

הרישום כבר מוגדר, כך שלא נדרשת הכנה נוספת.
