# הדרן קלאב · בריף פרויקט

מסמך רקע לסוכני קוד ומפתחים חדשים שנכנסים לפרויקט. מה המוצר, מי הקהל, מה הדגשים בכתיבת קוד ותוכן.

---

## 1. מה זה הדרן קלאב

**הדרן קלאב** הוא מועדון הטבות וחיסכון של רשת הדרן, המופעל על ידי **קהילות קארד**. הצעת הערך של המועדון היא מוצר יחיד וברור:

> **הדרן קארד** — כרטיס פיזי אחד שמוריד **5% הנחה מיידית בקופה**, בכל בית עסק שותף, בכל קנייה.

### מה מייחד את המוצר

- **מיידית, לא צבירה**. ההנחה יורדת מסך החשבון בקופה — לא נקודות, לא קופון, לא זיכוי עתידי.
- **כרטיס פיזי אחד**. אין אפליקציה, אין ברקוד לסרוק, אין טעינה מראש. מציגים את הכרטיס — הקופאי מזהה — 5% פחות.
- **בלי מינימום, בלי תקרה, בלי תוקף**. ההנחה חלה על הסל כולו, ברוב המקרים גם על מחירי מבצע.
- **פרסונליזציה משפחתית**. במסלול השנתי כלול כרטיס נוסף לבן/בת הזוג, שני הכרטיסים תחת חברות אחת.

### מודל תמחור

| מסלול | מחיר | הערות |
| --- | --- | --- |
| שנתי | ₪249 | כרטיס נוסף לבן/בת הזוג כלול |
| חודשי | ₪29 | ללא התחייבות, ניתן לבטל בכל עת |

שני המסלולים מקבלים את אותם 5% בכל בתי העסק השותפים. הכרטיס נשלח עד הבית תוך חמישה ימי עסקים ללא עלות משלוח.

---

## 2. קהל היעד

**המשפחה החרדית המסורתית**, שרוב הקנייה שלה קורית בשכונות ובערים ספציפיות:

- בני ברק
- ירושלים
- בית שמש
- מודיעין עילית
- אלעד
- ביתר עילית

הפרסונות המרכזיות (מיוצגות בעמוד הבית תחת "למי זה מיועד"):

1. **משפחה שרוצה לחסוך** — הצטרפות חדשה למועדון, לא בהכרח קשור להדרן.
2. **לקוח הדרן קיים** — מקבל את הכרטיס כהטבה נוספת ורואה חיסכון מצטבר באזור האישי.
3. **בעל עסק** — נכנס למועדון כדי להיחשף לתנועת הלקוחות הקבועה של הקהילה.

### קטגוריות מסחר (מפת בתי העסק)

התאמה מדויקת לסל הקנייה של הקהילה:

- רשתות מזון ומכולת (הנפוץ ביותר — בני ברק, ירושלים, בית שמש)
- מכולות שכונתיות
- בשר, עוף ודגים
- ביגוד והנעלה (בעיקר לילדים)
- ספרי קודש ויודאיקה
- כלי בית וריהוט
- צעצועים ומתנות
- פארמה וטיפוח
- אופטיקה
- צרכי כתיבה ומשרד

---

## 3. עקרונות מוצר

### הצגה שקופה

המחשבון בעמוד הבית, סעיף "סל קניות אחד, לפני ואחרי", ותצוגת החיסכון המצטבר באזור האישי — כולם משדרים אותו מסר: **המספרים אמיתיים ומיידיים**. אין star-footnotes שמצמצמות את ההבטחה. הסייגים היחידים המותרים הם אלה שמופיעים בפועל בקופה (פיקוח מחירים, מבצעים ייחודיים של בית עסק) — ותמיד מצוינים לצד ההנחה, לא בשוליים.

### שפה שמכבדת את הקהילה

- **עברית מלאה, RTL מלא**. לא ערבוב לועזי כמותג ראשי, לא אנגלית ב-UI copy.
- **תאריכים עבריים**. פעילות המועדון מוצגת בתאריכי חודשים עבריים (תמוז, סיוון), שנים בגימטריה (תשפ״ד, תשפ״ו).
- **מכובד ולא מתחנחן**. לא סלנג, לא emoji ב-UI. הטון של הצעת הערך: "מגיע לכם שקט נפשי גם בקניות."
- **פונים בלשון רבים** — "מציגים בקופה", "מזמינים את הכרטיס". לא ציווי.

### פשטות מעל הכל

מוצר אחד, מסר אחד. אין מסלולי הטבות מרובים, אין רמות חברות, אין מטבע פנימי. **כרטיס אחד = 5% בקופה**. כל תוספת ל-UI צריכה להיבחן מול העיקרון הזה — אם היא מפזרת קשב מהפעולה המרכזית (הזמנה / הפעלה / מציאת בית עסק שותף), היא כנראה מיותרת.

---

## 4. מפת האתר

| נתיב | ייעוד | פעולה עיקרית |
| --- | --- | --- |
| `/` | דף הבית — הצגת המועדון, ההצעה, בתי העסק, קהלי היעד | קבלת כרטיס |
| `/benefits` | רשימת 16 קטגוריות של בתי עסק שותפים, מסונן לפי קטגוריה ועיר | דפדוף וסינון |
| `/activate` | הפעלת כרטיס שהתקבל בדואר | הזנת מספר כרטיס + פרטים אישיים |
| `/balance` | בדיקת יתרה וחיסכון מצטבר לפני התחברות | מספר כרטיס + 4 ספרות ת״ז |
| `/merchants` | Landing לבעלי עסקים — למה להצטרף, איך זה עובד, טופס | טופס הצטרפות עסק |
| `/faq` | 20+ שאלות ותשובות ב-5 קבוצות (ההנחה, הכרטיס, בתי עסק, החברות, הכל) | Accordion |
| `/member` | אזור אישי לחברי מועדון | ניהול משפחה, כרטיסים, היסטוריית קניות לפי חודש עברי |
| `/search` | חיפוש רוחבי — בתי עסק, עמודים, שאלות | קלט חופשי; גם כ-Dialog גלובלי |

---

## 5. Content model — התוכן שנשמר בקוד

התוכן הסטטי חי ב-`lib/data/`, לא בעמודים עצמם. **כל שינוי תוכן תמיד מתחיל שם**, לא ב-JSX.

| קובץ | תוכן |
| --- | --- |
| `lib/data/site.ts` | פריטי תפריט, כותרות ועמודות ה-footer, שורות משפטיות ("תשפ״ו", "מופעל על ידי קהילות קארד"), FOOTER_NOTE |
| `lib/data/partners.ts` | רשימת בתי עסק שותפים (קטגוריה + עיר + מספר סניפים) |
| `lib/data/faq.ts` | קבוצות + שאלות + תשובות. כל שאלה משויכת לקבוצה אחת. |
| `lib/data/member.ts` | היסטוריית קניות למשפחת דמו לפי חודשים עבריים |
| `lib/data/activity.ts` | טיפוס `Purchase` המשמש ב-member.ts |
| `lib/data/search.ts` | האינדקס שהחיפוש הרוחבי סורק |

### כללי כתיבת תוכן

- **תמיד לרבים**. "מזמינים", "מציגים", "רואים". אף פעם לא "אתה תזמין".
- **מספרים בשקלים, עם ₪ צמוד**. אין רווח בין הסימן לספרה: `₪249`, לא `₪ 249`.
- **הנחה = 5%**. תמיד. לא "עד 5%", לא "5% ומעלה". המוצר הוא בדיוק 5%.
- **תאריכי דמו** — עבריים. פורמט: "כ״א בתמוז", "ט׳ בתמוז".
- **שנים** — בגימטריה בעברית ("תשפ״ד"), למעט טכני (`2025-…` ב-datetime).
- **מיקומים** — שמות היישובים המלאים בעברית (בני ברק, ירושלים, בית שמש, מודיעין עילית, אלעד, ביתר עילית).
- **סייגים** — "בכפוף לתקנון המועדון. ט.ל.ח." בסוף גרפיקות עם מספרים.

---

## 6. מיתוג ויזואלי

### צבעים

- **Ink** (שחור-כחלחל עמוק) — הרקע של הכרטיס עצמו, ה-CTA הסוגר, כותרות ראשיות.
- **Gold** (זהב חם, `--gold-500` … `--gold-700`) — הצבע המותגי הבולט. משמש בבורדר של הכרטיס (BorderBeam), במספרים גדולים (`5%` ב-Hero), בסמל הציטוט.
- **Sand** (שמנת חמה) — רקעי סקשנים משניים, כרטיסים בטון רך.
- **Canvas / Canvas-soft** — רקעי בסיס לבנים.
- **Primary-deep** — טקסט מודגש בטון המותג.
- **Positive** — ירוק לחיסכון (`−₪66` באדום? לא — בירוק כדי להדגיש שהחיסכון חיובי).

**אין hardcode של צבעים ב-JSX.** תמיד `[var(--color-*)]` דרך Tailwind arbitrary values.

### טיפוגרפיה

- **Afek (א.א.א)** — הפונט המותגי, weights 400–800. נטען מקומית מ-`public/fonts/`. משמש גם לכותרות (800) וגם לגוף (400/500). פולבק: Heebo → Assistant.
- **Frank Ruhl Libre** — סריף טקסי מ-Google Fonts. **שימוש מוגבל: לא יותר מפעמיים בעמוד**, ורק לפול-קוואטס (ציטוט לקוח בעמוד הבית).
- **Numeric** — כל המספרים ב-`tnum` (tabular numerals) כדי שהעמודות במחשבון ובאזור האישי יתיישרו לרוחב.

### רמת תנועה

- **Hero** — ללא scroll-reveal. הוא מעל-הקפל, אנימציה מעכבת LCP.
- **סקשנים אחרים** — עטופים ב-`<Reveal>` עם fade-in-up רך. עם `stagger` — הילדים נחשפים בהדרגה קלה.
- **BorderBeam** — קרן זהב סביב כרטיס החבר ב-Hero. **רגע מיתוגי אחד בלבד בעמוד.**
- **NumberTicker** — הספרות ברצועת ה-KPI עולות מ-0 בזמן הכניסה לתצוגה.
- **AnimatedShinyText** — shimmer דיסקרטי על תג ה-"5% הנחה מיידית בקופה" ב-Hero.
- **אין parallax, אין scroll-hijacking, אין קרוסלות אוטומטיות.**

---

## 7. עקרונות טכניים

### RTL

האתר עברית מלאה: `<html lang="he" dir="rtl">` ב-`app/layout.tsx`. `RtlProvider` מזרים את הכיוון ל-Radix primitives (Dialog, Select, Tabs) כדי שהפופאפים ייפתחו לצד הנכון בעברית.

### Server Components כברירת מחדל

`"use client"` **רק** כשמצריך אינטראקטיביות מקומית — טפסים, Radix primitives, motion, event handlers. עמודי תוכן שנרנדרים סטטית לא צריכים client boundary.

### Design tokens

CSS custom properties ב-`styles/tokens/*.css` הם המקור היחיד לצבע/פונט/רווח/רדיוס/אנימציה. Tailwind arbitrary values (`[var(--radius-2xl)]`) מקשרים אותם ל-utility classes. **אין hex ב-JSX. אין ערכי pixels קשיחים לרווחים גדולים** — משתמשים ב-`clamp()` עם `vw` כדי לקבל טיפוגרפיה נוזלית.

### Handoff parity

המקור הוויזואלי הוא `project/*.dc.html` (Claude Design bundle). כל `page.tsx` מקבילי צריך לשמור על **הסדר, ההיררכיה והמלל** של ה-HTML המקביל. שינויי מלל = החלטה מודעת של ה-owner, לא "שיפור" של ה-agent.

### Motion budget

תזוזה = תשומת לב. כל אנימציה שנוספת גוזלת קשב מהמסר. סף עליון: **רגע-קסם אחד לעמוד** (ה-BorderBeam ב-Hero), ואחר כך רק פידבק פונקציונלי (Reveal על סקשנים, NumberTicker על KPIs).

### Accessibility

- כל ה-icons דקורטיביים עטופים ב-`aria-hidden` דרך `<Icon>`.
- כפתורי icon-only מקבלים `aria-label` דרך `<IconButton>`.
- ניגודיות: הטקסט העיקרי על canvas עומד ב-WCAG AA. הזהב **לא** משמש לטקסט קטן על רקע לבן (רק ל-accents ולמספרים גדולים).
- הכרטיס ב-Hero הוא `<div>` דקורטיבי; המידע ("משפחת כהן", "חבר מועדון") לא ה-CTA — ה-CTA הוא הכפתור "קבלת הדרן קארד".

---

## 8. מה **לא** לעשות

- ❌ לא לשנות "5%" לערך אחר בשום מקום ב-copy.
- ❌ לא להוסיף מטבע פנימי / נקודות / רמות חברות.
- ❌ לא לתרגם את האתר לאנגלית ולא להוסיף language switcher.
- ❌ לא להוסיף אנימציית BorderBeam יותר מפעם אחת בעמוד.
- ❌ לא להוסיף hard-coded hex לצבעים.
- ❌ לא לשנות את הפונט המותגי או להוסיף פונטים נוספים ללא סיבה חזקה.
- ❌ לא להזיז את `project/` — הוא ה-visual source of truth וה-build כבר מוחרג ממנו.
- ❌ לא להוסיף מבצעים מוגבלים בזמן, ספירה לאחור, popup הצטרפות, או exit-intent modals. המוצר עומד לבד; לא צריך fomo tactics.
- ❌ לא להשתמש ב-emoji ב-UI copy או ב-code comments שנשמרים בקוד.

---

## 9. סדר עדיפויות בעת שינוי

1. **שמירה על ה-parity** מול `project/*.dc.html` המקביל.
2. **שמירה על ה-copy** בדיוק כפי שהוא — מלל, סדר, מספרים.
3. **שמירה על ה-design tokens** — לא לצייר בצבע שלא קיים במערכת.
4. **Performance** — RSC כברירת מחדל, Hero ללא animation, פונטים עם `display: swap`.
5. **Accessibility** — קונטרסט, aria labels, focus states.
6. רק אז — פיצ׳רים חדשים.
