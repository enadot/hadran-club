# הדרן קלאב · בריף פרויקט

מסמך רקע לסוכני קוד ומפתחים חדשים שנכנסים לפרויקט. מגדיר מה המוצר, מי הקהל, מה השפה ומה הדגשים בכתיבת קוד ותוכן. **מסמך זה הוא ה-source of truth האסטרטגי** — כאשר יש סתירה בין הבריף כאן לבין טקסטים ישנים שנשארו בקוד, הבריף מנצח והקוד צריך להתעדכן בהתאם.

---

## 1. מה זה הדרן קלאב

**הדרן קלאב** הוא מועדון הלקוחות ה"פרימיום" של הדרן, המופעל טכנולוגית על ידי **קהילות קארד**. הוא לא רק מספק הנחות — הוא מייצר **כוח קנייה קהילתי** שמאפשר להשיג תנאים שמשתמש בודד לא יכול להשיג לבדו.

> **המועדון סגור ומיועד ללקוחות קיימים של הדרן בלבד.** הוא אינו נמכר, אינו מגויס ואינו פתוח להרשמה. הכרטיס מגיע כחלק מהקשר עם הדרן — ולכן **אין באתר הזמנת כרטיס, אין טופס הצטרפות ואין הרשמה למועדון**.

> **הזמנת הכרטיס מתבצעת ב"אפליקציית שירות ותמיכה"** — אפליקציה שקיימת בכל מכשירי הדרן. האתר רק מציין זאת, במקומות שבהם משתמש היה מחפש טופס. הקבוע: `CARD_ORDER_CHANNEL` ב-`lib/data/site.ts`.

> **אין באתר טלפון ואין בו כתובת דוא״ל — בשום עמוד.** כל פנייה של חבר עוברת דרך אותה אפליקציית שירות ותמיכה (`SUPPORT_CHANNEL`). ערוץ הפנייה היחיד שהאתר מפעיל בעצמו הוא טופס בעלי העסקים.

> **הדרן קארד** — כרטיס פיזי אחד שנותן גישה לרשת רחבה של הטבות משתנות, מהנחות בסיסיות ליום-יום ועד לדילים בעומק של עשרות אחוזים בחנויות בלעדיות שסגורות למי שאינו חבר.

### שלושת הבידולים המרכזיים

1. **עומק ההטבה** — טווח הנחות רחב, לא מספר קבוע. **החל מהנחות בסיסיות ועד לעשרות אחוזים** ברשתות ובקטגוריות נבחרות. ההנחה משתנה לפי בית עסק.
2. **בלעדיות** — יש חנויות ורשתות שבהן ההטבה זמינה **אך ורק דרך הדרן קלאב**. זו נקודת הזהב של המועדון: לא רק הנחה, אלא גישה.
3. **גב טכנולוגי** — הישענות על המערכת המוכחת של **קהילות קארד**. הכרטיס הפיזי, אזור אישי, חיוב הקניות — כל הגלגלים כבר מסתובבים; המשתמש רק נהנה מהתוצאה.

### מודל החברות

**החברות במועדון ללא עלות וכלולה בהיות המשתמש לקוח הדרן.** אין דמי חבר, אין דמי חידוש, אין מסלולים ואין רמות חברות — כרטיס אחד, גישה אחת, טווח הטבות דינמי. כרטיס נוסף לבן/בת הזוג כלול ללא תוספת תשלום, וניתן לעזוב בכל עת ללא דמי ביטול.

**אין להחזיר תמחור ל-UI** (מסלול שנתי/חודשי, ₪249, ₪29, "מחיר", "דמי חבר"), **ואין להחזיר מסלול הזמנה או טופס הצטרפות למועדון.**

> **אין סקשן חברות בדף הבית, ואין `MembershipCard`.** הפאנל ההצהרתי ("חינם לכל לקוחות הדרן")
> הוסר, ואיתו `lib/data/membership.ts` ו-`components/site/MembershipCard.tsx`. העובדה עצמה
> נשארת באתר במקומות שבהם באמת שואלים אותה — שאלה ייעודית ב-`lib/data/faq.ts` וסעיף
> "עלות החברות" בתקנון (`lib/data/legal.ts`). **אין להחזיר את הפאנל לדף הבית.**

### מה האתר עושה

אתר ייצוגי סביב שלושה עמודים, ולא משפך גיוס:

1. **הטבות** — מי השותפים, מה ההטבה בכל אחד ואילו חנויות בלעדיות למועדון (`/benefits`).
2. **בדיקת יתרה** — יתרה וסטטוס לפי מספר כרטיס בלבד (`/balance`).
3. **אזור אישי** — קישור יוצא למערכת של קהילות קארד.

לצדם שתי פעולות משנה: **הפעלת כרטיס** שהתקבל (`/activate`) ו**פנייה של בעלי עסקים** (`/merchants`).

> **הפעלת הכרטיס רצה כרגע במסך של קהילות קארד, בתוך IFRAME.** ה-endpoint הציבורי
> `POST /public/card-activate` עדיין אינו קולט הפעלות עבור המועדון, ולכן `/activate` ממסגר את
> המסך של המפעיל (`CARD_ACTIVATION_URL` ב-`lib/data/site.ts`) והטופס של המועדון ממתין ב-`/archive/activate`.
> **אין למחוק את הארכיון** — הוא חוזר לשמש את `/activate` ברגע שההפעלה תעבוד ב-API.

> **המסגרת היא cross-origin.** אי אפשר לקרוא את ה-DOM שלה, למדוד את גובהה, לזהות שליחה
> מוצלחת או לערוך את המלל שבתוכה. חיתוך ה-chrome של המפעיל (התגית, הכותרת וסרגל השלבים
> שמעל הטופס) נעשה בדרך היחידה שקיימת — מסגרת גבוהה מהחלון הנראה, שנדחפת מעלה מאחורי clip —
> ולכן הוא **כבוי כברירת מחדל ומכויל ב-env**, לא בקוד: `NEXT_PUBLIC_CARD_ACTIVATION_CROP_TOP`,
> `…_CROP_BOTTOM`, `…_FRAME_HEIGHT`. הפתרון היציב הוא שהמפעיל יגיש גרסת embed בלי ה-chrome שלו.

---

## 2. קהל היעד

**לקוחות הדרן** — המשפחה החרדית המסורתית, שרוב הקנייה שלה קורית בשכונות ובערים ספציפיות:

- בני ברק
- ירושלים
- בית שמש
- מודיעין עילית
- אלעד
- ביתר עילית

הפרסונות המרכזיות:

1. **לקוח הדרן שקיבל כרטיס** — מפעיל אותו, מחפש איפה הוא עובד ובודק יתרה.
2. **חבר ותיק במועדון** — רואה חיסכון מצטבר באזור האישי ומחפש חנויות חדשות ברשימה.
3. **בעל עסק** — נכנס למועדון כדי להיחשף לתנועת הלקוחות הקבועה של הקהילה. הפרסונה היחידה שיש לה טופס באתר.

**אין פרסונת "מצטרף חדש".** מי שאינו לקוח הדרן אינו קהל היעד של האתר, ואין לכתוב אליו copy.

### קטגוריות מסחר (מפת בתי העסק)

התאמה מדויקת לסל הקנייה של הקהילה:

- רשתות מזון ומכולת
- מכולות שכונתיות
- בשר, עוף ודגים
- ביגוד והנעלה (בעיקר לילדים)
- ספרי קודש ויודאיקה
- כלי בית וריהוט
- צעצועים ומתנות
- פארמה וטיפוח
- אופטיקה
- צרכי כתיבה ומשרד

---

## 3. עקרונות מוצר וטון (Voice & Tone)

### יוקרתי אך נגיש

המותג צריך להרגיש כמו "מועדון זהב" — אבל כזה שכל משפחה חרדית יכולה ורוצה להיות חלק ממנו. **לא סנוב, לא מנופח.** הזהב שבפלטה הוא רמז לאיכות, לא לבלעדיות אריסטוקרטית. הכרטיס מכובד, לא יהיר.

### אמין ופרקטי

**בלי הבטחות שווא.** מדברים במספרים ריאליים, בשמות חנויות שהמשתמש מכיר, ובחיסכון שמרגישים בסוף החודש — לא בתאוריה. כשמציגים טווח הנחות ("עד 30%"), מציגים גם מקרים ספציפיים לצידו כדי שהמשתמש יבין שזה לא תעלול שיווקי.

### קהילתי

השפה מוטית לגוף ראשון רבים ולתחושת שותפות:

- **"בלעדי לחברי הדרן קלאב"** — לכל מקום שבו מסבירים למה ההטבות האלה קיימות. **החליף את "הנבחרת שלנו"**, שהיה מטבע לשון פנימי ולא אמר דבר למי שפוגש את הכרטיס בפעם הראשונה. אין להחזיר אותו ל-UI.
- **"השותפים שלנו"** — לבתי העסק בכלל.
- **"הקהילה שלנו"** — לחברי המועדון.
- **"מזמינים", "מציגים", "רואים"** — לפניות לקהל. אף פעם לא "אתה תזמין", לא "לחצו כאן".

### פשטות של חוויה

התהליך הוא **אפס מאמץ**: הכרטיס מגיע ללקוח הדרן → הפעלה מהירה באתר → הצגה בקופה. **ההנחה מיידית** — לא צבירת נקודות, לא זיכוי עתידי, לא בקשה לשליחת קבלה. מציגים — יורד — משלמים פחות.

---

## 4. מפת האתר

| נתיב | ייעוד | פעולה עיקרית |
| --- | --- | --- |
| `/` | דף הבית — הצגת המועדון, עומק ההטבות, הבלעדיות, איך זה עובד, ומה אפשר לעשות באתר | מעבר לרשימת בתי העסק |
| `/benefits` | "מנוע החיסכון האישי" — **שער הכרטיס פותח את העמוד** (אין PageHero), ומתחתיו רשימת בתי העסק | הזנת 8 ספרות אחרונות, חיפוש וסינון, ולחיצה על בית עסק לפתיחת ההטבה |
| `/activate` | הפעלת כרטיס שהתקבל — **IFRAME ברוחב מלא** של מסך ההפעלה של קהילות קארד (`CARD_ACTIVATION_URL`), עם כותרת ורצועת "כדאי לדעת" משל האתר | הפעלה בתוך המסגרת, וכפתור "פתיחה בחלון נפרד" שגלוי תמיד |
| `/archive/activate` | ארכיון — טופס ההפעלה של המועדון עצמו, שמור לשימוש עתידי. `noindex` | אינו ערוץ ההפעלה הפעיל; חוזר ל-`/activate` ביום שה-endpoint יתוקן |
| `/balance` | בדיקת יתרה וסטטוס כרטיס לפני התחברות | מספר כרטיס בלבד — ה-API הציבורי של קהילות קארד מזהה לפי 8 הספרות האחרונות ואינו מחזיר פרטים אישיים. **אין טעינת כרטיס בעמוד.** |
| `/merchants` | Landing לבעלי עסקים — למה להצטרף, איך זה עובד | כפתור שפותח **Dialog** עם טופס ההצטרפות (`MerchantJoinDialog`) |
| `/faq` | שאלות ותשובות ב-5 קבוצות (ההנחה, הכרטיס, בתי עסק, החברות, הכל) | Accordion |
| `/search` | חיפוש רוחבי — בתי עסק, עמודים, שאלות | קלט חופשי; גם כ-Dialog גלובלי |
| `/terms` | תקנון המועדון | מסמך קריאה |
| `/privacy` | מדיניות פרטיות | מסמך קריאה |
| `/terms-of-use` | תנאי שימוש | מסמך קריאה |

שלושת המסמכים המשפטיים חיים ב-`lib/data/legal.ts` ומוצגים דרך `components/site/LegalDocument.tsx`. הנוסח טרם עבר בדיקה משפטית — `REVIEW_NOTE` אומר זאת בראש כל עמוד, ומחיקתו היא שורה אחת.

**האזור האישי אינו עמוד באתר.** הוא מתופעל על ידי קהילות קארד במערכת שלהם. כל אזכור שלו ב-UI הוא
קישור יוצא לכתובת שב-`MEMBER_AREA_URL` (`lib/data/site.ts`, נשלט ב-`NEXT_PUBLIC_MEMBER_AREA_URL`),
הנפתח בלשונית חדשה עם אייקון `external-link`. אין להחזיר עמוד `/member` לאתר.

---

## 5. סקשנים בעמוד הבית — אפיון תוכן

### סקשן 1 · Hero (הרושם הראשוני)

- **הרעיון:** הצגת הכרטיס כ"מפתח" לעולם של חיסכון.
- **דגש תוכן:** שילוב בין המותג המוכר (הדרן) לבין הערך החדש (חיסכון). המסר: זהו שלב אבולוציוני של הקהילה — מהגנה טכנולוגית להגנה על הכיס. הפנייה היא ללקוח הדרן, לא למצטרף.
- **המסר המרכזי:** *"מועדון ההטבות של לקוחות הדרן — החיסכון שמרגישים בסוף החודש."*
- **CTA:** לרשימת בתי העסק (ראשי) + הפעלת כרטיס (משני). **לא** "להזמנת כרטיס".

### סקשן 2 · עומק וטווח ההטבות (The Value)

- **הרעיון:** שבירת המיתוס של "הנחה קטנה ואחידה".
- **דגש תוכן:** ההנחות דינמיות. יש חנויות עם הנחה בסיסית ליום-יום, ויש דילים עמוקים של עשרות אחוזים במוקדי הקנייה המרכזיים. חשוב להראות דוגמאות קונקרטיות ולא רק להצהיר.
- **המסר המרכזי:** *"מהנחות קבועות ועד עשרות אחוזים — הכרטיס שמתאים את עצמו לסל הקניות שלכם."*

### סקשן 3 · נבחרת החנויות והבלעדיות (Exclusivity)

- **הרעיון:** הדרן קלאב כ"מועדון סגור".
- **דגש תוכן:** הצגת לוגואים של רשתות מובילות, תוך הדגשה של חנויות שהטבתן זמינה **רק לחברי הדרן קלאב**. זה מייצר תחושת FOMO לגיטימית — לא מבצע-בזק שנגמר בעוד שעתיים, אלא הבנה שהגישה עצמה תלויה בחברות.
- **המסר המרכזי:** *"רשתות מובילות וחנויות בלעדיות שתוכלו למצוא רק אצלנו."*

### סקשן 4 · איך זה עובד (The Process)

- **הרעיון:** "אפס מאמץ".
- **דגש תוכן:** תהליך של 3 שלבים — הכרטיס מגיע ללקוחות הדרן → הפעלה מהירה באתר → הצגה בקופה. להדגיש שההנחה **מיידית**, בלי המתנה, בלי בירוקרטיה. השלב הראשון אינו פעולה שהמשתמש מבצע ואין לו CTA.
- **המסר המרכזי:** *"פשוט מציגים את הכרטיס וחוסכים במקום."*

### סקשן 5 · מה אפשר לעשות כאן

- **הרעיון:** האתר ייצוגי, ולכן הסקשן הזה הוא עמוד השדרה שלו.
- **דגש תוכן:** ארבעה כרטיסים — רשימת בתי העסק, בדיקת יתרה, האזור האישי (קישור יוצא), ובעלי עסקים.
- **רקע:** רצועת ה-sand. הסקשן הזה ירש את ההפרדה שסקשן החברות היה נותן לפניו.

### סקשן 6 · סגירה

- **דגש תוכן:** הכרטיס כבר אצל החבר. ה-CTA הסוגר הוא **האזור האישי**, לא הזמנה.

### שאלות נפוצות (`/faq`, Barrier Removal)

- **דגש תוכן:** מי יכול להיות חבר (לקוחות הדרן); איך יודעים כמה הנחה יש בכל חנות (עמוד ההטבות באתר); מה עושים אם הכרטיס אבד; איך עוזבים.

---

## 6. עמוד רשימת הטבות (`/benefits`) — אפיון

**קונספט:** "מנוע החיסכון האישי שלי."

**לעמוד שני מצבים, ומקור נתונים אחד לכל מצב:**

1. **ויטרינה** — בלי כרטיס, הרשימה היא הדירקטוריון הסטטי ב-`lib/data/partners.ts`: מי השותפים, ותו לא. אין בו הטבות, כי ההטבה אינה זהה לכל חבר.
2. **מנוע אישי** — עם כרטיס, `GET /public/benefits/by-card/:card_code` מחזיר את הרשימה של **אותו כרטיס**, וכל בית עסק בה נושא את **ההטבה בלשון בית העסק עצמו**, דגל בלעדיות ורשימת סניפים. הרשימה כולה מתחלפת, ואיתה גם אפשרויות הסינון.

**מבנה רעיוני:**

- **שער הכרטיס** בראש העמוד (`components/site/BenefitsGate.tsx`) — הרצועה היחידה בעמוד שיושבת על שלב הזהב של סולם המשטחים. "גלו את ההנחות שמגיעות לכם", שדה של 8 ספרות, וכפתור "הצגת ההטבות שלי". אחרי טעינה הוא מתכווץ לשורת סטטוס עם מספר בתי העסק והכרטיס הממוסך. **זו ההנעה לפעולה של העמוד — אין שנייה בתחתיתו.**
- **מספר הכרטיס נשמר ב-`sessionStorage` בלבד**, כדי שמעבר לעמוד אחר וחזרה לא יבקש אותו שוב. **הוא לעולם אינו נכנס ל-URL** — הסינון ניתן לשיתוף, כרטיס לא.
- **סרגל הסינון הוא שורה אחת.** מתחת ל-900px: שדה חיפוש + כפתור אחד עם מונה הסינונים הפעילים, שפותח את `components/site/FilterSheet.tsx` (מיון, סוג הטבה, קטגוריה, עיר). מעל 900px הפיקרים גלויים בשורה. **אין להחזיר ארבעה פקדים מוערמים ל-sticky** — נמדד: 303px של chrome קבוע מתחת לניווט, חצי ממסך של 360×740.
- **הסרגל מתחבא בגלילה למטה וחוזר בגלילה למעלה** (מתחת ל-900px), ומכבד `prefers-reduced-motion`.
- **המונה והסינונים הפעילים אינם sticky.** הם יושבים מעל הרשת, כל סינון כצ׳יפ עם × להסרה.
- **חיפוש חופשי** לפי שם חנות, קטגוריה או עיר. עם כרטיס טעון הוא סורק גם את מלל ההטבה עצמו ("כפל מבצעים", "עדשות").
- **הפיקרים נגזרים מהרשימה שעל המסך**, לא מוצהרים: לשני המקורות אין אותן ערים ואותן קטגוריות.
- **תגית "בלעדי להדרן קלאב"** על חנויות שההטבה בהן זמינה רק לחברים — בולטת ויזואלית (זהב על ink), לא מוסתרת בשוליים. מגיעה מדגל `exclusive` של ה-API.
- **הכרטיסייה לא מציגה תצוגה מקדימה של ההטבה** — רק שם, קטגוריה, פריסה וכפתור "הצגת ההטבה". בתי העסק כותבים את המלל באורך חופשי, וקיצוץ שלו לשתי שורות חתך את ההנחה עצמה באמצע המשפט.
- **לחיצה על בית עסק פותחת Dialog** עם ההטבה בפאנל זהב משלה — המשפט הראשון בגודל תצוגה, התנאים מתחתיו — ואחריה רשימת הסניפים (שם, כתובת, שעות). **בלי טלפונים** — ה-API מחזיר אותם, וה-route handler מפיל אותם לפני שהם מגיעים לדפדפן.
- **סוג ההטבה (`tier`) נקבע רק מ-`exclusive`.** שאר הדרגות מתארות עומק, וה-API מנסח את ההטבה כטקסט חופשי — דירוג "מורחבת" לפי אחוז שמופיע במלל הוא המצאה של האתר, לא הצהרה של בית העסק.

---

## 7. Content model — התוכן שנשמר בקוד

התוכן הסטטי חי ב-`lib/data/`, לא בעמודים עצמם. **כל שינוי תוכן תמיד מתחיל שם**, לא ב-JSX.

| קובץ | תוכן |
| --- | --- |
| `lib/data/site.ts` | פריטי תפריט, כותרות ועמודות ה-footer, שורות משפטיות ("תשפ״ו", "מופעל על ידי קהילות קארד"), FOOTER_NOTE, MEMBER_AREA_URL, CARD_ACTIVATION_URL |
| `lib/data/partners.ts` | הדירקטוריון הסטטי — רשימת בתי עסק שותפים (קטגוריה + עיר + לוגו). **בלי הטבות:** ההטבה אינה זהה לכל חבר, והיא מגיעה מה-API לפי כרטיס. |
| `lib/data/live-benefits.ts` | התפר בין ה-API לדירקטוריון: טיפוסי ה-wire של `/api/card/benefits`, נרמול ערים, מיפוי הקטגוריות של המפעיל לאוצר המילים של המועדון, והמרה של חנות חיה ל-`Partner`. |
| `lib/data/faq.ts` | קבוצות + שאלות + תשובות. כל שאלה משויכת לקבוצה אחת. |
| `lib/data/search.ts` | האינדקס שהחיפוש הרוחבי סורק |

### כללי כתיבת תוכן

- **תמיד לרבים.** "מזמינים", "מציגים", "רואים". אף פעם לא "אתה תזמין".
- **שפה קהילתית.** "השותפים שלנו", "הקהילה שלנו". **לא "הנבחרת שלנו"** — ראו סעיף 3.
- **בהירות לפני מטבעות לשון.** כותרת שצריך להסביר אינה כותרת. אם ניסוח מותגי לא מובן למי שמגיע לראשונה, הניסוח הפשוט מנצח.
- **טווחי הנחות, לא מספר יחיד.** "עד 30%", "בין 5% ל-25%", "בלעדי — עד 40%". לא לפרסם ערך יחיד גורף שאינו מדויק.
- **מספרים בשקלים, עם ₪ צמוד.** אין רווח בין הסימן לספרה: `₪120`, לא `₪ 120`.
- **תאריכי דמו** — עבריים. פורמט: "כ״א בתמוז", "ט׳ בתמוז".
- **שנים** — בגימטריה בעברית ("תשפ״ד"), למעט טכני (`2025-…` ב-datetime).
- **מיקומים** — שמות היישובים המלאים בעברית (בני ברק, ירושלים, בית שמש, מודיעין עילית, אלעד, ביתר עילית).
- **סייגים** — "בכפוף לתקנון המועדון. ט.ל.ח." בסוף גרפיקות עם מספרים.
- **בלעדיות מוצגת במפורש.** כשחנות היא "רק דרך הדרן קלאב", זה מודגש בתגית ויזואלית, לא נחבא במלל.

---

## 8. מיתוג ויזואלי

### צבעים

- **Ink** (שחור-כחלחל עמוק) — הרקע של הכרטיס עצמו, ה-CTA הסוגר, כותרות ראשיות. מייצר את תחושת ה-"פרימיום".
- **Gold** (זהב חם, `--gold-500` … `--gold-700`) — הצבע המותגי הבולט. משמש בבורדר של הכרטיס (BorderBeam), במספרים גדולים, בסמל הציטוט, ובתגיות "בלעדי".
- **Sand** (שמנת חמה) — רקעי סקשנים משניים, כרטיסים בטון רך.
- **Canvas / Canvas-soft** — רקעי בסיס לבנים.
- **Primary-deep** — טקסט מודגש בטון המותג.
- **Positive** — ירוק לחיסכון (כדי להדגיש שהחיסכון חיובי, לא תשלום שלילי).

**אין hardcode של צבעים ב-JSX.** תמיד `[var(--color-*)]` דרך Tailwind arbitrary values.

### סולם המשטחים (surface ladder)

העומק באתר נישא על ידי **גוון, לא על ידי צל**. כל משטח יושב שלב אחד מהמשטח שמאחוריו,
וכל השלבים הם על אותו ראמפ חם — ולכן שום דבר לא נקרא כפלטה שנייה:

| שלב | טוקן | ערך | שימוש |
| --- | --- | --- | --- |
| 0 | `--surface-0` / `--color-canvas` | לבן | רקע הדף |
| 1 | `--surface-1` / `--color-canvas-warm` | `--sand-100` | הגוון השקט — משטחי קריאה ארוכים (מסמכים משפטיים) |
| 2 | `--surface-2` / `--color-canvas-soft` | `--sand-200` | גוון הסקשן הרגיל |
| 3 | `--surface-3` / `--color-canvas-pale` | `--gold-50` | השלב הראשון שיש בו מותג — **רצועה אחת בעמוד**, לסקשן שהעמוד באמת עליו |
| 4 | `--surface-4` / `--color-canvas-wash` | `--gold-100` | הגוון החם ביותר, לפאנל בודד |
| — | `--color-canvas-ink` | `--ink-900` | המשטח הכהה היחיד בעמוד (footer, או פרומו סוגר אחד) |

**כרטיס לבן על רצועה לבנה אינו כרטיס.** לפני שמניחים פאנל, בודקים שהרצועה מתחתיו נמצאת
שלב אחד משם.

### צללים

`box-shadow` שמור **לשכבות צפות בלבד** — Dialog, drawer, פאנל של Select. על רקע קרם חם צל
אפור נקרא כלכלוך מתחת לקופסה, ולא כהרמה. במקומו: `--ring-hairline`, `--ring-gold`,
`--ring-gold-soft`, `--ring-ink` ב-`styles/tokens/elevation.css`. `--shadow-card`,
`--shadow-raised` ו-`--shadow-gold` נשארו כ-aliases בלבד ואין להשתמש בהם בקוד חדש.

### סולם ה-tracking

ה-tracking מתהדק ככל שהטיפוגרפיה גדלה, לפי גודל ולא לפי אלמנט:
`--tracking-display-sm` (24–32px), `--tracking-display` (40–48px),
`--tracking-display-lg` (56–68px), `--tracking-display-xl` (80px+).

### טיפוגרפיה

- **Afek (א.א.א)** — הפונט המותגי, weights 400–800. נטען מקומית מ-`public/fonts/`. משמש גם לכותרות (800) וגם לגוף (400/500). פולבק: Heebo → Assistant.
- **Frank Ruhl Libre** — סריף טקסי מ-Google Fonts. **שימוש מוגבל: לא יותר מפעמיים בעמוד**, ורק לפול-קוואטס (ציטוט לקוח בעמוד הבית).
- **Numeric** — כל המספרים ב-`tnum` (tabular numerals) כדי שהעמודות במחשבון ובאזור האישי יתיישרו לרוחב.

### רמת תנועה

- **Hero** — ללא scroll-reveal; ה-LCP הוא תמונת הכרטיס ואין לעכב אותה. קיר הלוגואים שמאחוריו הוא היוצא מן הכלל היחיד, והוא נטען עצל ואינו חוסם ציור.
- **סקשנים אחרים** — עטופים ב-`<Reveal>` עם fade-in-up רך. עם `stagger` — הילדים נחשפים בהדרגה קלה.
- **BorderBeam** — קרן זהב סביב כרטיס החבר ב-Hero. **רגע מיתוגי אחד בלבד בעמוד.**
- **NumberTicker** — הספרות ברצועת ה-KPI עולות מ-0 בזמן הכניסה לתצוגה.
- **AnimatedShinyText** — shimmer דיסקרטי על תג ה-Hero.
- **אין parallax, אין scroll-hijacking, אין קרוסלות אוטומטיות.**

### לוגואים של רשתות

הצגת לוגואים של הרשתות המובילות היא חלק מהותי מהמסר (סקשן 3 בעמוד הבית + עמוד `/benefits`). ה-handoff הנוכחי לא כולל אותם — כשיגיעו, הם מונחים על plate ניטרלי (canvas-soft) כדי לא להשתלט על הפלטה המותגית.

---

## 9. עקרונות טכניים

### RTL

האתר עברית מלאה: `<html lang="he" dir="rtl">` ב-`app/layout.tsx`. `RtlProvider` מזרים את הכיוון ל-Radix primitives (Dialog, Select, Tabs) כדי שהפופאפים ייפתחו לצד הנכון בעברית.

**כל שדות הקלט הם RTL, כולל ה-placeholder שלהם.** אין `dir="ltr"` על `Input`. placeholder עברי בשדה LTR נשבר — "8 ספרות אחרונות" נצבע "ספרות אחרונות 8".

**היוצא מן הכלל היחיד: שדה שמחזיק מספר מקובץ ברווחים** (מספר כרטיס מלא, שמודפס בקבוצות של ארבע). אלגוריתם ה-bidi פותר רווח בין שני רצפים מספריים לכיוון הפסקה, ולכן `4271 1111 2222 8032` נצבע `8032 2222 1111 4271` — מספר הכרטיס של החבר, מוצג לו הפוך, בדיוק בשדה שבו הוא בודק שהקליד נכון. לשדות האלה יש `.numeric-field` ב-`app/globals.css`: הערך נשאר בסדר לטיני והשדה עדיין מיושר לימין. **רק לשדות בלי placeholder עברי** — מחרוזת עברית ברצף LTR נשברת באותה צורה בכיוון ההפוך.

### Server Components כברירת מחדל

`"use client"` **רק** כשמצריך אינטראקטיביות מקומית — טפסים, Radix primitives, motion, event handlers. עמודי תוכן שנרנדרים סטטית לא צריכים client boundary.

### Design tokens

CSS custom properties ב-`styles/tokens/*.css` הם המקור היחיד לצבע/פונט/רווח/רדיוס/אנימציה. Tailwind arbitrary values (`[var(--radius-2xl)]`) מקשרים אותם ל-utility classes. **אין hex ב-JSX.**

### ה-API של קהילות קארד

כל קריאה עוברת דרך route handler ב-`app/api/card/*` (`lib/api/kehilot.ts`), כדי שה-host, המפתח והלוגים יישארו בצד השרת. שלושה endpoints ציבוריים בשימוש:

| Endpoint | שימוש | הערות |
| --- | --- | --- |
| `GET /public/card-balance/:card_code` | `/balance` | מזוהה לפי **8 הספרות האחרונות**. מחזיר `card_status` ו-`total_balance` בלבד. |
| `GET /public/benefits/by-card/:card_code` | `/benefits` | מזוהה לפי **8 הספרות האחרונות**. מחזיר `stores[]` — `name`, `logo_url` (יחסי ל-host), `category_name`, `benefit_text`, `exclusive`, `pinned`, `branch_count`, `cities[]`, `branches[]`. |
| `POST /public/card-activate` | `/archive/activate` בלבד (**לא בשימוש ב-`/activate`** — ראו למעלה) | חובה: `card_code` (המספר **המלא** שעל הכרטיס), `full_name`, `phone`. רשות: `israeli_id`, `email`, `birthdate` (YYYY-MM-DD), `address`, `city`, `gender` (`זכר`/`נקבה`). מחזיר `card_code_masked`, `holder_name`, `status`. שדות רשות ריקים **אינם נשלחים**. |

**אין endpoint לטעינה ואין endpoint להזמנת כרטיס.** אלה לא פעולות שהאתר מבצע.

> **מה שה-route handler של ההטבות מפיל בדרך, ולא במקרה:** `club_name`, `club_logo_url`
> ו-`benefits_slug` הם הזהות של הארגון המפעיל במערכת של קהילות קארד — הם עלולים
> להיות שם של ארגון אחר לגמרי, ואין להם מקום ב-UI של חבר הדרן (סעיף 10).
> `balance` ו-`points` הם ארנק, ויש להם עמוד משלהם. `branches[].phone` יורד לפי הכלל
> שאין באתר טלפון בשום עמוד. **אין להחזיר אף אחד מהם לתשובה.**

### Handoff parity

המקור הוויזואלי הוא `project/*.dc.html` (Claude Design bundle). כל `page.tsx` שומר על הסדר, ההיררכיה והמלל של ה-HTML המקביל. **שינויי מלל חייבים להתיישר עם הבריף האסטרטגי כאן** — לא "שיפורים" אקראיים של agent.

הפרוטוטייפים מקדימים למעבר למועדון סגור, ולכן הם עדיין מכילים הזמנת כרטיס וטעינת כרטיס. **במקומות האלה הבריף מנצח וה-parity נשבר במכוון.**

### Motion budget

תזוזה = תשומת לב. סף עליון: **רגע-קסם אחד לעמוד**, ואחר כך רק פידבק פונקציונלי (Reveal על סקשנים, NumberTicker על KPIs).

בדף הבית הרגע הזה הוא **קיר הלוגואים הנע מאחורי הכרטיס ב-Hero** (`HeroLogoWall` מעל `DriftWall` של React Bits) — החלטה מכוונת שגוברת על הכלל הישן של "Hero ללא אנימציה". הוא דקורטיבי לחלוטין: `aria-hidden`, בלי tab stops, `pointer-events: none`, מוסתר מתחת ל-560px, ועוצר תחת `prefers-reduced-motion`. ה-BorderBeam ירד מהעמוד עם כרטיס ה-CSS שהוא הקיף. **אין להוסיף רגע שני.**

### Accessibility

- כל ה-icons דקורטיביים עטופים ב-`aria-hidden` דרך `<Icon>`.
- כפתורי icon-only מקבלים `aria-label` דרך `<IconButton>`.
- ניגודיות: הטקסט העיקרי על canvas עומד ב-WCAG AA. הזהב **לא** משמש לטקסט קטן על רקע לבן (רק ל-accents ולמספרים גדולים).
- הכרטיס ב-Hero הוא תמונה דקורטיבית (`alt=""`); ה-CTA הוא הכפתור "לרשימת בתי העסק".

---

## 10. White-label — מה להצניע

הדרן קלאב הוא ה-front, קהילות קארד הוא ה-back. הפרדה חשובה בממשק:

- ✅ **מותר לציין** — "מופעל על ידי קהילות קארד" ב-footer, גיבוי טכנולוגי בקצרה בעמוד `/merchants`.
- ❌ **לא להיכנס אליו ב-UI של הצרכן** — מנגנוני גיוס ארגונים, מודל B2B2C, שיתופי פעולה עם ארגונים אחרים של קהילות קארד. הכל צריך להישאר מאחורי הקלעים.
- **הפוקוס בכל טקסט המופנה לצרכן** — חוויית המשתמש הסופי: המשפחה שמחזיקה כרטיס וחוסכת בקנייה הקרובה.

---

## 11. מה **לא** לעשות

- ❌ לא לפרסם מספר הנחה יחיד גורף (כמו "5% בכל מקום"). **ההנחה טווח.** אם צריך ערך יחיד לצורך אמצעי הדגמה, כותבים "החל מ-" או "עד ל-".
- ❌ לא להוסיף מטבע פנימי / נקודות / רמות חברות. **כרטיס אחד, גישה אחת, טווח הטבות דינמי.**
- ❌ לא להחזיר **הזמנת כרטיס, הרשמה או טופס הצטרפות לצרכן** — לא כעמוד, לא כמסלול בטופס ולא כ-CTA ("להזמנת כרטיס", "הצטרפות", "קבלת כרטיס"). המועדון סגור ללקוחות הדרן. הטופס היחיד המופנה החוצה הוא של **בעלי עסקים**.
- ❌ לא להחזיר **טעינת כרטיס** (`TopUpPanel`, `/api/card/topup`). `/balance` הוא בדיקה בלבד, לא משטח תשלום.
- ❌ לא לתרגם את האתר לאנגלית ולא להוסיף language switcher.
- ❌ לא להוסיף אנימציית BorderBeam יותר מפעם אחת בעמוד.
- ❌ לא להוסיף hard-coded hex לצבעים.
- ❌ לא להחזיר `box-shadow` לכרטיסים. העומק הוא סולם המשטחים והטבעות (`--ring-*`); צל שמור לשכבות צפות בלבד.
- ❌ לא להחזיר `PageHero` ל-`/benefits`. הוא חזר על כותרת העמוד ודחף את שער הכרטיס מתחת לקיפול בטלפון. ה-`h1` נשאר שם כ-`sr-only`.
- ❌ לא להעמיס את סרגל הסינון ב-`/benefits`. שורה אחת מתחת ל-900px; כל השאר ב-`FilterSheet`.
- ❌ לא לכתוב header משלו לעמוד משנה **אחר**. יש `components/site/PageHero.tsx` — eyebrow, כותרת, lead, meta, actions ו-aside, עם ה-wash המשותף.
- ❌ לא לכתוב eyebrow ידני (`text-[13px] font-bold tracking-wide`). יש `<Eyebrow>` ב-`components/site/Band.tsx`, והקו הזהב שלו הוא חלק מהסימן.
- ❌ לא למחוק את `/archive/activate` ולא להסיר ממנו את ה-`noindex`.
- ❌ לא לשנות את הפונט המותגי או להוסיף פונטים נוספים ללא סיבה חזקה.
- ❌ לא להזיז את `project/` — הוא ה-visual source of truth וה-build כבר מוחרג ממנו.
- ❌ לא להוסיף **מבצעי-בזק מזויפים** — ספירה לאחור של "המבצע נגמר בעוד שעתיים", pop-ups של exit-intent, וקישוטי דחיפות מלאכותיים. FOMO של בלעדיות אמיתית (חנות שקיימת רק לחברים) — **מותר**; דחיפות מומצאת — **אסור**.
- ❌ לא לחשוף את מנגנוני קהילות קארד לצרכן (ראו סעיף 10).
- ❌ לא להחזיר **טלפון או דוא״ל** לשום עמוד — לא כטקסט, לא כ-`tel:` ולא כ-`mailto:`.
- ❌ לא להשתמש ב-emoji ב-UI copy או ב-code comments שנשמרים בקוד.

---

## 12. סדר עדיפויות בעת שינוי

1. **התאמה לבריף האסטרטגי כאן** — מועדון סגור ללקוחות הדרן, טווח הטבות, בלעדיות, שפה קהילתית. אם הקוד עדיין משקף את המסרנות הישנה של "5% קבוע" או של מועדון פתוח להרשמה, זה חוב שיש לצמצם.
2. **שמירה על ה-parity** מול `project/*.dc.html` המקביל, כל עוד הוא לא סותר את הבריף.
3. **שמירה על ה-design tokens** — לא לצייר בצבע שלא קיים במערכת.
4. **Performance** — RSC כברירת מחדל, Hero ללא animation, פונטים עם `display: swap`.
5. **Accessibility** — קונטרסט, aria labels, focus states.
6. רק אז — פיצ׳רים חדשים.
